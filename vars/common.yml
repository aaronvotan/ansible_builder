---
ee_base_image: registry.redhat.io/ansible-automation-platform-24/ee-minimal-rhel8:latest
ee_base_registry_username: "{{ redhat_registry_username }}"
ee_base_registry_password: "{{ redhat_registry_password }}"
ee_registry_dest: "{{ custom_ee_registry_dest | default('automationhub.uscis.dhs.gov', true) }}"
ee_ah_host: "{{ custom_ee_registry_dest | default('automationhub.uscis.dhs.gov', true) }}"
ee_ah_token: "{{ uscis_ah_token }}"
ee_registry_username: "{{ hub_service_user }}"
ee_registry_password: "{{ hub_service_pass }}"
ee_ca_cert_dir: "{{ custom_ee_cacert_dir | default('cacerts'), true}}"
ee_nexus_registry: nexus-nonprod-gss.uscis.dhs.gov:8123/aap
